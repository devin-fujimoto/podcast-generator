name: "Podcast Generator"
author: "Devin Fujimoto"
description: "stuff"
runs:
  using: "docker"
  image: "dockerFile"
branding:
  icon: "git-branch"
  color: "red"
inputs:
  email: 
    description: Tho committer's email address
    required: true
    default: ${{ github.actor }}@localhost
  name:
    description: The comitter's name
    required: true
    default: ${{ github.actor }}